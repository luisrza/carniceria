{
  "name": "carniceria",
  "version": "1.0.0",
  "description": "La Carnicería — orden + tracker en tiempo real",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
